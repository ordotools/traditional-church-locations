{
  "name": "traditional-church-locations",
  "version": "0.1.0",
  "private": true,
  "description": "A map of traditional Mass centers, with an admin panel for adding pins by address or by scraping a URL.",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "cheerio": "^1.0.0",
    "compression": "^1.8.2",
    "ejs": "^3.1.10",
    "express": "^4.21.0",
    "express-session": "^1.19.0"
  }
}
